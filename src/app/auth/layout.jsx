export const metadata = {
  title: {
    default: "Authentication - ZendzQR",
    template: "%s - ZendzQR Authentication"
  },
  robots: {
    index: false,
    follow: false,
  }
}

export default function AuthLayout({ children }) {
  return children
}